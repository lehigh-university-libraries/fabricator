name: Create release
on:
  pull_request_target:
    branches:
      - main
    types:
      - closed
jobs:
  release:
    if: github.event.pull_request.merged == true && !contains(github.event.pull_request.title, 'skip-release')
    uses: lehigh-university-libraries/gha/.github/workflows/bump-release.yaml@c8210d733114d47b7cbb55cefe87466f60b811a5 # main
    with:
      workflow_file: goreleaser.yml
      prefix: v
    permissions:
      contents: write
      actions: write
    secrets: inherit
